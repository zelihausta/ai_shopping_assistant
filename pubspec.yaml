name: ai_shoppinng_assistant
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  image_picker: ^1.0.7
  image_cropper: ^7.0.2
  path: ^1.9.0
  path_provider: ^2.1.2
  flutter:
    sdk: flutter

  image: ^3.3.0
  cupertino_icons: ^1.0.8
  camera: ^0.11.2
  permission_handler: ^12.0.1
  flutter_tts: ^3.8.3
  provider: ^6.1.2
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/
